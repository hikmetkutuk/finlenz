pre-commit:
  parallel: true
  commands:
    prettier:
      glob: "frontend/src/**/*.{ts,tsx}"
      run: npx --prefix frontend prettier --write {staged_files}
      stage_fixed: true

    gofmt:
      glob: "backend/**/*.go"
      run: gofmt -w {staged_files}
      stage_fixed: true
